import React from "react";
// REDUX
import { useDispatch } from "react-redux";
import { loadGames } from "../actions/gameAction";

const Home = () => {
    return(
        <div>
            <h2>Leo</h2>
        </div>
    );
};

export default Home;
